-- TODO: Join products through product_id and include the product code.
-- Do not use tickets.product_code: this query must survive its removal.
select t.id, t.product_id, t.price, t.currency
from tickets t
order by t.id;
